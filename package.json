{
  "scripts": {
    "teste":"echo \"Error: no test specified\" && exit 1",
    "start": "lite-server",
    "watch": "tsc --watch",
    "tsc": "tsc"
  },
  "dependencies": {
    "lite-server": "^2.6.1"
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
